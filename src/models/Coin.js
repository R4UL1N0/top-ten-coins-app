class Coin {
    constructor(obj) {
        this.id = obj.id
        this.name = obj.name
        this.sign = obj.sign
        this.price = obj.price
        this.image = obj.image
    }
}

export default Coin